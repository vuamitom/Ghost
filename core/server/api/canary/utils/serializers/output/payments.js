const common = require('../../../../../lib/common');
const mapper = require('./utils/mapper');

module.exports = {
    payWithMomo(models, apiConfig, frame) {
        frame.response = models;
    }
};
